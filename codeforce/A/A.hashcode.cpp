#include<iostream>
using namespace std;
#include<vector>
#define ll long long
int main()
{
	ll book,libraries,scanning_days;
	ll score[book];
	ll l_book[libraries],l_process_time[libraries],book_per_day[library];
	for(int i=0;i<book;++i)
	cin>>score[i];
	vector < ll >l_books[libraries];
	int x; 
	for(int i=0;i<libraries;++i)
	{
		cin>>l_book[i]>>l_process_time[i]>>book_per_day[i];
		for(int j=0;j<l_book[i];++j)
		{
			cin>>x;
			v[i].push_back(x);
		}
	}	
}
