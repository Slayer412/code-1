#include<iostream>
using namespace std;
int main()
{
    int t;cin>>t;
    while(t--)
    {   long int pen,pencile;
        long int lec,prectical,npen,npencil,max;
        cin>>lec>>prectical>>npen>>npencile>>max;
        if(lec%pen)
            pen=lec/npen+1;
        else
            pen=lec/npen;
        if(prectical%npencil)
            pencil=prectical/npencile + 1;
        else
            pencil=prectical/npencil;
        if(pen+pencil<=max)
            c
    }
}
